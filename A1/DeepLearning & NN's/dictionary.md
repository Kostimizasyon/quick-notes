INVARIANCE => The act of not changing when a change happesn => x(a + b = x) ==== xa + xb = x*x

RE-Scaling => Changing something are you dumb?

RESIDUAL CONNECTIONS => Residual connections (skip connections) let a layer's input bypass the layer and get added directly to its output || x = F(x) + x

ASYMPTOT => EDGING THE GRAPH (Asimtot)

0 SHOT => The model knowing something before it ever saw it.
